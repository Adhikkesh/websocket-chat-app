import Button from "../ui/Button";

export default function Entry(){
    return (
        <div className="flex justify-center items-center h-screen gap-4">
            <Button content="Join" onClick={() => {}}/>
            <Button content="Chat" onClick={() => {}}/>
        </div>
    )
}